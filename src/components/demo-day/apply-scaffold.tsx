/** Minimal header accent — lighter than the main Demo Day landing. */
export function DemoApplyScaffold() {
  return (
    <div
      aria-hidden
      className="pointer-events-none absolute inset-x-0 top-0 h-px bg-[var(--demo-accent)]/15"
    />
  );
}
